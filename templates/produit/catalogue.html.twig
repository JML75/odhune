{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Catalogue {% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/catalogue.css') }}">
{% endblock %}
{% block h1 %}Catalogue produit{% endblock %}
{% block content %}

<div class="wrapper">
     {# <nav aria-label="Breadcrumb" class="breadcrumb">
        <div>
            <ul>
                <li><a href="{{ path('accueil') }}">Accueil</a></li>
                <li><a  href="{{ path ('catalogue') }}">Produits</a></li>
                {% if categorie == 'Tous' %}
                <li><span aria-current="page"><a class="categorie" href="{{ path ('catalogue') }}">{{ categorie }}</a></span></li>
                    
                {% else %}
                <li><span aria-current="page"><a class="categorie" href="{{ path (categorie|lower) }}">{{ categorie }}</a></span></li>
                {% endif %}
            
            </ul>
    </div>
    </nav> #}
    <div class="d-none d-lg-block">
        <nav  aria-label="Breadcrumb" class="breadcrumb">
        
                <ul>
                    <li class="d-flex flex-column align-items-center"><a  href="{{ path ('optique') }}">Optiques</a>
                    {% if categorie == 'Optique' %}
                        <div class="bouton my-2"></div>
                    {% endif %}
                    </li>
                        
                    <li class="d-flex flex-column align-items-center"><a  href="{{ path ('solaire') }}">Solaires</a>
                    {% if categorie == 'Solaire' %}
                        <div class="bouton my-2"></div>
                    </li>
                    {% endif %}
                    <li class="d-flex flex-column align-items-center"><a  href="{{ path ('capsule') }}">Edition limitée</a>
                    {% if categorie == 'Capsule' %}
                        <div class="bouton my-2"></div>
                        </li>
                    {% endif %}
                </ul>
        </nav>
    </div>





 {% if produits %} {# {% if produits is not empty %} #}
 
        <div class="row d-flex flex-wrap content ">

            {% for produit in produits %}

                <div class="col-lg-4 col-sm-6 col-12 carte">        
                    <a class ='lienFicheProduit' href="{{ path('fiche_produit', {'id' : produit.id}) }}">
                        <img class ='photoProduit' src="{{ asset("img/photoProduitUpload/" ~  produit.getPhotoProduits.first() ) }}" alt="image du produit" style="width:100%">
                        <p class="nomProduit">{{ produit.nom|upper }}</p>
                  </a>         
                </div>   
                            
            {% endfor %}

        </div>

    {% else %}{# si le tableau produits est vide #}
        <div class="col-12 d-flex w-100 mt-5 pt-5 mx-auto" style="width:100%">

        <h2 class=" col-6 text-center text-danger mt-3 fst-italic mx-auto pt-5 mt-5 ">Il n'y a aucun produit pour le moment</h2>
        </div>
        
</div>
        


    {% endif %}
  

{% endblock %}
